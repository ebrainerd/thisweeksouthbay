{
  "name": "this-week-south-bay",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "astro dev --port 43177 --host",
    "build": "astro build && node scripts/assert-min-pages.mjs && cp dist/sitemap-index.xml dist/sitemap.xml",
    "preview": "astro preview --port 43177 --host",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "@fontsource/public-sans": "^5.3.0",
    "@vercel/analytics": "^1.6.1",
    "astro": "^7.2.10"
  }
}
